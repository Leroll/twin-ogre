# TwinOgre

> One head talks. One head thinks.

A dual-plane runtime for real-time voice agents, one head keeps talking while the other reasons, plans, and uses tools in the background.

TwinOgre is an open-source experiment for building non-blocking conversational agents. It separates real-time interaction from long-running reasoning so an agent can stay responsive while handling complex tasks.
